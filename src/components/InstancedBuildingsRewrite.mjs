
import fs from 'fs';
fs.writeFileSync('c:/Users/admin/Desktop/work/fun/github city/src/components/InstancedBuildings.tsx', "\n\"use client\";\n\nimport { useRef, useMemo, useEffect } from \"react\";\nimport { useFrame, useThree } from \"@react-three/fiber\";\nimport * as THREE from \"three\";\nimport type { CityBuilding } from \"@/lib/github\";\nimport type { BuildingColors } from \"./CityCanvas\";\n\nconst MAX_RISE_TOTAL = 5.0; // max seconds for all buildings to rise\nconst RISE_DURATION = 1.6;  // seconds per building\n\nconst vertexShader = `\nattribute vec4 aUvFront;\nattribute vec4 aUvSide;\nattribute float aRise;\nattribute vec4 aTint;\n\nvarying vec2 vUv;\nvarying vec3 vNormal;\nvarying vec4 vInstanceColor;\nvarying vec4 vUvFront;\nvarying vec4 vUvSide;\nvarying float vRise;\nvarying vec4 vTint;\nvarying vec3 vWorldPosition;\n\nvoid main() {\n    vUv = uv;\n    vNormal = normalMatrix * mat3(instanceMatrix) * normal;\n    vInstanceColor = instanceColor;\n    vUvFront = aUvFront;\n    vUvSide = aUvSide;\n    vRise = aRise;\n    vTint = aTint;\n\n    vec3 pos = position;\n\n    // Apply rise animation\n    float pivotY = -0.5; \n    pos.y -= pivotY;\n    pos.y *= aRise;\n    pos.y += pivotY;\n\n    vec4 worldPos = instanceMatrix * vec4(pos, 1.0);\n    vWorldPosition = worldPos.xyz;\n    gl_Position = projectionMatrix * viewMatrix * worldPos;\n}\n`;\n\nconst fragmentShader = `\nuniform vec3 uFogColor;\nuniform float uFogNear;\nuniform float uFogFar;\nuniform float uTime;\nuniform sampler2D uAtlas;\nuniform float uFocusedId;\nuniform float uFocusedIdB;\nuniform vec3 uAccentCol;\nuniform float uEmissiveIntensity;\n\nvarying vec2 vUv;\nvarying vec3 vNormal;\nvarying vec4 vInstanceColor;\nvarying vec4 vUvFront;\nvarying vec4 vUvSide;\nvarying float vRise;\nvarying vec4 vTint;\nvarying vec3 vWorldPosition;\n\n// Pseudo-random function for noise\nfloat random(vec2 st) {\n    return fract(sin(dot(st.xy, vec2(12.9898,78.233))) * 43758.5453123);\n}\n\nvoid main() {\n    if (vRise < 0.01) discard;\n\n    vec3 n = normalize(vNormal);\n    vec3 col = vInstanceColor.rgb;\n\n    // Tint\n    col = mix(col, vTint.rgb, vTint.a);\n\n    bool isWall = abs(n.y) < 0.1;\n    bool isFront = abs(n.z) > abs(n.x);\n    vec4 uvTarget = isFront ? vUvFront : vUvSide;\n\n    // Windows mapping\n    if (isWall) {\n        float fU = uvTarget.x;\n        float fV = uvTarget.y;\n        float sU = uvTarget.z;\n        float sV = uvTarget.w;\n\n        float repU = vUv.x * fU;\n        float repV = vUv.y * fV;\n        vec2 atlasUv = vec2(fract(repU) * sU, fract(repV) * sV);\n        vec4 texel = texture2D(uAtlas, atlasUv);\n\n        float edgeDistU = min(fract(repU), 1.0 - fract(repU));\n        float edgeDistV = min(fract(repV), 1.0 - fract(repV));\n        float frameGrout = 0.04;\n\n        if (edgeDistU > frameGrout && edgeDistV > frameGrout) {\n             col = mix(col, texel.rgb * 1.5, texel.a);\n        } else {\n             col *= 0.6; // window frames\n        }\n    } else {\n        // Roof\n        col *= 0.8;\n    }\n\n    // Directional lighting\n    vec3 lightDir = normalize(vec3(0.5, 1.0, 0.3));\n    float ndotl = max(dot(n, lightDir), 0.0);\n    vec3 ambient = vec3(0.2);\n    col *= ambient + ndotl * 0.8;\n\n    gl_FragColor = vec4(col, 1.0);\n\n    // Exponential Fog\n    #ifdef USE_FOG\n        float depth = gl_FragCoord.z / gl_FragCoord.w;\n        float fogFactor = smoothstep(uFogNear, uFogFar, depth);\n        gl_FragColor.rgb = mix(gl_FragColor.rgb, uFogColor, fogFactor);\n    #endif\n}\n`;\n\nfunction createMaterial(colors: BuildingColors, atlasTexture: THREE.Texture) {\n  return new THREE.ShaderMaterial({\n    vertexShader,\n    fragmentShader,\n    uniforms: {\n      uTime: { value: 0 },\n      uAtlas: { value: atlasTexture },\n      uFogColor: { value: new THREE.Color() },\n      uFogNear: { value: 0 },\n      uFogFar: { value: 0 },\n      uFocusedId: { value: -1 },\n      uFocusedIdB: { value: -1 },\n      uAccentCol: { value: new THREE.Color(colors.accent) },\n      uEmissiveIntensity: { value: 1.0 },\n    },\n    fog: true,\n  });\n}\n\ninterface Props {\n  buildings: CityBuilding[];\n  colors: BuildingColors;\n  atlasTexture: THREE.Texture;\n  focusedBuilding?: string | null;\n  focusedBuildingB?: string | null;\n  introMode?: boolean;\n  onBuildingClick?: (building: CityBuilding) => void;\n  holdRise?: boolean;\n  emissiveIntensity?: number;\n}\n\ninterface RiseState {\n  idx: number;\n  startTime: number;\n}\n\nlet hasPlayedRiseGlobal = false;\n\n// We refactor the component to map buildings into exactly 3 Biome arrays,\n// then render 3 InstancedMesh components.\nexport default function InstancedBuildings({\n  buildings,\n  colors,\n  atlasTexture,\n  focusedBuilding,\n  focusedBuildingB,\n  introMode = false,\n  onBuildingClick,\n  holdRise = false,\n  emissiveIntensity = 1.0,\n}: Props) {\n  // 1. Group buildings by biome\n  const { frontend, backend, dataAi } = useMemo(() => {\n    const f: CityBuilding[] = [];\n    const b: CityBuilding[] = [];\n    const d: CityBuilding[] = [];\n    for (const bld of buildings) {\n      if (bld.biome === \"frontend\") f.push(bld);\n      else if (bld.biome === \"data_ai\") d.push(bld);\n      else b.push(bld);\n    }\n    return { frontend: f, backend: b, dataAi: d };\n  }, [buildings]);\n\n  return (\n    <group>\n        {frontend.length > 0 && <BiomeInstancedMesh buildings={frontend} biome=\"frontend\" colors={colors} atlasTexture={atlasTexture} focusedBuilding={focusedBuilding} focusedBuildingB={focusedBuildingB} introMode={introMode} onBuildingClick={onBuildingClick} holdRise={holdRise} emissiveIntensity={emissiveIntensity} />}\n        {backend.length > 0 && <BiomeInstancedMesh buildings={backend} biome=\"backend\" colors={colors} atlasTexture={atlasTexture} focusedBuilding={focusedBuilding} focusedBuildingB={focusedBuildingB} introMode={introMode} onBuildingClick={onBuildingClick} holdRise={holdRise} emissiveIntensity={emissiveIntensity} />}\n        {dataAi.length > 0 && <BiomeInstancedMesh buildings={dataAi} biome=\"data_ai\" colors={colors} atlasTexture={atlasTexture} focusedBuilding={focusedBuilding} focusedBuildingB={focusedBuildingB} introMode={introMode} onBuildingClick={onBuildingClick} holdRise={holdRise} emissiveIntensity={emissiveIntensity} />}\n    </group>\n  );\n}\n\nconst _position = new THREE.Vector3();\nconst _quaternion = new THREE.Quaternion();\nconst _scale = new THREE.Vector3();\nconst _matrix = new THREE.Matrix4();\nconst _color = new THREE.Color();\n\n// Component to handle a single InstancedMesh\nfunction BiomeInstancedMesh({\n  buildings,\n  biome,\n  colors,\n  atlasTexture,\n  focusedBuilding,\n  focusedBuildingB,\n  introMode,\n  onBuildingClick,\n  holdRise,\n  emissiveIntensity\n}: Props & { biome: string }) {\n  const meshRef = useRef<THREE.InstancedMesh>(null);\n  const count = buildings.length;\n\n  // Setup Geometry based on Biome\n  const geo = useMemo(() => {\n    if (biome === \"frontend\") {\n      // Sleek, glassy skyscrapers (Standard box but distinct look handled in shaders/materials ideally)\n      return new THREE.BoxGeometry(1, 1, 1);\n    } else if (biome === \"data_ai\") {\n      // Hexagonal crystals\n      return new THREE.CylinderGeometry(0.5, 0.5, 1, 6);\n    } else {\n      // Backend: Brutalist, complex block (using Box for performance, but distinct material properties)\n      return new THREE.BoxGeometry(1, 1, 1);\n    }\n  }, [biome]);\n\n  // Setup Material based on Biome\n  const material = useMemo(() => {\n    const mat = createMaterial(colors, atlasTexture);\n    return mat;\n  }, [colors, atlasTexture]);\n\n  useEffect(() => {\n    if (material.uniforms) {\n       material.uniforms.uEmissiveIntensity.value = emissiveIntensity;\n    }\n  }, [emissiveIntensity, material]);\n\n  // Precompute Attributes\n  const { uvFrontData, uvSideData, tintData, riseData, loginToIdx } = useMemo(() => {\n    const rise = new Float32Array(count);\n    const tint = new Float32Array(count * 4);\n    const uvF = new Float32Array(count * 4);\n    const uvS = new Float32Array(count * 4);\n    const map = new Map<string, number>();\n\n    for (let i = 0; i < count; i++) {\n      const b = buildings[i];\n      map.set(b.login.toLowerCase(), i);\n\n      let tintC = _color.setHex(0x000000);\n      let tintA = 0.0;\n      if (b.custom_color) {\n        tintC = _color.set(b.custom_color);\n        tintA = 0.5; // Custom color blend\n      } else if (biome === \"frontend\") {\n        tintC = _color.setHex(0x00ffff); // Neon cyan\n        tintA = 0.2;\n      } else if (biome === \"backend\") {\n        tintC = _color.setHex(0x333333); // Dark brutalist\n        tintA = 0.6;\n      } else if (biome === \"data_ai\") {\n        tintC = _color.setHex(0xff00ff); // Purple crystal\n        tintA = 0.3;\n      }\n      \n      tint[i * 4] = tintC.r;\n      tint[i * 4 + 1] = tintC.g;\n      tint[i * 4 + 2] = tintC.b;\n      tint[i * 4 + 3] = tintA;\n\n      uvF[i * 4] = b.windowsPerFloor;\n      uvF[i * 4 + 1] = b.floors;\n      uvF[i * 4 + 2] = b.claimed ? 0.5 : 1.0; \n      uvF[i * 4 + 3] = 1.0;\n\n      uvS[i * 4] = b.sideWindowsPerFloor;\n      uvS[i * 4 + 1] = b.floors;\n      uvS[i * 4 + 2] = b.claimed ? 0.5 : 1.0;\n      uvS[i * 4 + 3] = 1.0;\n\n      rise[i] = 0;\n    }\n    return { uvFrontData: uvF, uvSideData: uvS, tintData: tint, riseData: rise, loginToIdx: map };\n  }, [buildings, count, biome]);\n\n  const riseInitialized = useRef(false);\n  const risingRef = useRef<RiseState[]>([]);\n  const holdRiseRef = useRef(holdRise);\n  holdRiseRef.current = holdRise;\n\n  useEffect(() => {\n    const mesh = meshRef.current;\n    if (!mesh) return;\n\n    for (let i = 0; i < count; i++) {\n        const b = buildings[i];\n        _position.set(b.position[0], b.height / 2, b.position[2]);\n        _scale.set(b.width, b.height, b.depth);\n        _matrix.compose(_position, _quaternion, _scale);\n        mesh.setMatrixAt(i, _matrix);\n        \n        let c = _color.set(colors.base);\n        if (b.claimed) c = _color.set(colors.claimed);\n        if (biome === \"frontend\") c.lerp(_color.setHex(0x00ffff), 0.2);\n        if (biome === \"data_ai\") c.lerp(_color.setHex(0xff00ff), 0.2);\n        if (biome === \"backend\") c.lerp(_color.setHex(0x111111), 0.4);\n        mesh.setColorAt(i, c);\n    }\n    mesh.instanceMatrix.needsUpdate = true;\n    if (mesh.instanceColor) mesh.instanceColor.needsUpdate = true;\n\n    // Force coarse sphere\n    let maxDist = 0;\n    let maxHeight = 0;\n    for (let i = 0; i < count; i++) {\n      const b = buildings[i];\n      const d = Math.sqrt(b.position[0] * b.position[0] + b.position[2] * b.position[2]);\n      if (d > maxDist) maxDist = d;\n      if (b.height > maxHeight) maxHeight = b.height;\n    }\n    const radius = Math.sqrt(maxDist * maxDist + maxHeight * maxHeight) + 100;\n    mesh.boundingSphere = new THREE.Sphere(new THREE.Vector3(0, maxHeight / 2, 0), radius);\n    mesh.boundingBox = null;\n\n    const uvFrontAttr = new THREE.InstancedBufferAttribute(uvFrontData, 4);\n    const uvSideAttr = new THREE.InstancedBufferAttribute(uvSideData, 4);\n    const riseAttr = new THREE.InstancedBufferAttribute(riseData, 1);\n    riseAttr.setUsage(THREE.DynamicDrawUsage);\n    const tintAttr = new THREE.InstancedBufferAttribute(tintData, 4);\n\n    mesh.geometry.setAttribute(\"aUvFront\", uvFrontAttr);\n    mesh.geometry.setAttribute(\"aUvSide\", uvSideAttr);\n    mesh.geometry.setAttribute(\"aRise\", riseAttr);\n    mesh.geometry.setAttribute(\"aTint\", tintAttr);\n\n    if (hasPlayedRiseGlobal) {\n      for (let i = 0; i < count; i++) riseData[i] = 1;\n      riseAttr.needsUpdate = true;\n      riseInitialized.current = true;\n      risingRef.current = [];\n    } else {\n      riseInitialized.current = false;\n      risingRef.current = [];\n    }\n    mesh.count = count;\n  }, [buildings, count, uvFrontData, uvSideData, riseData, tintData, biome, colors]);\n\n  const lastFogNear = useRef(0);\n  const lastFogFar = useRef(0);\n  useFrame(({ scene }) => {\n    if (!material.uniforms) return;\n    const fog = scene.fog as THREE.Fog | null;\n    if (!fog) return;\n    if (fog.near !== lastFogNear.current || fog.far !== lastFogFar.current) {\n      material.uniforms.uFogColor.value.copy(fog.color);\n      material.uniforms.uFogNear.value = fog.near;\n      material.uniforms.uFogFar.value = fog.far;\n      lastFogNear.current = fog.near;\n      lastFogFar.current = fog.far;\n    }\n  });\n\n  useEffect(() => {\n    if (!material.uniforms) return;\n    const idA = focusedBuilding ? loginToIdx.get(focusedBuilding.toLowerCase()) : undefined;\n    const idB = focusedBuildingB ? loginToIdx.get(focusedBuildingB.toLowerCase()) : undefined;\n    material.uniforms.uFocusedId.value = idA !== undefined ? idA : -1.0;\n    material.uniforms.uFocusedIdB.value = idB !== undefined ? idB : -1.0;\n  }, [focusedBuilding, focusedBuildingB, loginToIdx, material]);\n\n  useFrame(({ clock }) => {\n    const mesh = meshRef.current;\n    if (!mesh) return;\n    if (holdRiseRef.current) return;\n\n    const now = clock.elapsedTime;\n    if (!riseInitialized.current) {\n      riseInitialized.current = true;\n      const staggerDelay = Math.min(0.003, MAX_RISE_TOTAL / Math.max(1, count));\n      const queue: RiseState[] = [];\n      for (let i = 0; i < count; i++) queue.push({ startTime: now + i * staggerDelay, idx: i });\n      risingRef.current = queue;\n    }\n\n    const rising = risingRef.current;\n    if (rising.length === 0) {\n      if (!holdRiseRef.current) hasPlayedRiseGlobal = true;\n      return;\n    }\n\n    const riseAttr = mesh.geometry.getAttribute(\"aRise\") as THREE.InstancedBufferAttribute;\n    if (!riseAttr) return;\n    const arr = riseAttr.array as Float32Array;\n\n    let anyChanged = false;\n    const nextRising: RiseState[] = [];\n\n    for (let r = 0; r < rising.length; r++) {\n      const state = rising[r];\n      const elapsed = now - state.startTime;\n      if (elapsed < 0) {\n        for (let j = r; j < rising.length; j++) nextRising.push(rising[j]);\n        break;\n      }\n      const progress = Math.min(1, elapsed / RISE_DURATION);\n      const t = 1 - Math.pow(1 - progress, 3);\n      arr[state.idx] = t;\n      anyChanged = true;\n\n      if (progress < 1) nextRising.push(state);\n    }\n    risingRef.current = nextRising;\n    if (anyChanged) riseAttr.needsUpdate = true;\n  });\n\n  const { gl, camera } = useThree();\n  const raycasterRef = useRef(new THREE.Raycaster());\n  const pointerNDC = useRef(new THREE.Vector2());\n\n  const buildingsRef = useRef(buildings);\n  buildingsRef.current = buildings;\n  const onClickRef = useRef(onBuildingClick);\n  onClickRef.current = onBuildingClick;\n  const introRef = useRef(introMode);\n  introRef.current = introMode;\n  const tapRef = useRef<{ time: number; id: number; x: number; y: number } | null>(null);\n\n  useEffect(() => {\n    const canvas = gl.domElement;\n    const screenToNDC = (clientX: number, clientY: number) => {\n      const rect = canvas.getBoundingClientRect();\n      pointerNDC.current.x = ((clientX - rect.left) / rect.width) * 2 - 1;\n      pointerNDC.current.y = -((clientY - rect.top) / rect.height) * 2 + 1;\n    };\n\n    const raycastInstance = (clientX: number, clientY: number): number | null => {\n      const mesh = meshRef.current;\n      if (!mesh) return null;\n      screenToNDC(clientX, clientY);\n      raycasterRef.current.setFromCamera(pointerNDC.current, camera);\n      const hits: THREE.Intersection[] = [];\n      mesh.raycast(raycasterRef.current, hits);\n      if (hits.length > 0) {\n        hits.sort((a, b) => a.distance - b.distance);\n        if (hits[0].instanceId !== undefined) return hits[0].instanceId;\n      }\n      return null;\n    };\n\n    const onPointerDown = (e: PointerEvent) => {\n      if (introRef.current) return;\n      if ((window as any).__spireClicked) return;\n      const id = raycastInstance(e.clientX, e.clientY);\n      if (id !== null && id < buildingsRef.current.length) {\n        tapRef.current = { time: performance.now(), id, x: e.clientX, y: e.clientY };\n      }\n    };\n\n    const onPointerUp = (e: PointerEvent) => {\n      const tap = tapRef.current;\n      if (!tap) return;\n      tapRef.current = null;\n      const elapsed = performance.now() - tap.time;\n      if (elapsed > 400) return;\n      const dx = e.clientX - tap.x;\n      const dy = e.clientY - tap.y;\n      if (dx * dx + dy * dy > 625) return;\n      if (tap.id < buildingsRef.current.length) {\n        onClickRef.current?.(buildingsRef.current[tap.id]);\n      }\n    };\n\n    const isTouch = \"ontouchstart\" in window || navigator.maxTouchPoints > 0;\n    let lastMoveTime = 0;\n    const onPointerMove = isTouch ? null : (e: PointerEvent) => {\n      if (introRef.current) {\n        document.body.style.cursor = \"auto\";\n        return;\n      }\n      if ((window as any).__spireCursor) return;\n      const now = performance.now();\n      if (now - lastMoveTime < 125) return;\n      lastMoveTime = now;\n      const id = raycastInstance(e.clientX, e.clientY);\n      if (id !== null) {\n        document.body.style.cursor = \"pointer\";\n      }\n    };\n\n    canvas.addEventListener(\"pointerdown\", onPointerDown);\n    window.addEventListener(\"pointerup\", onPointerUp);\n    if (onPointerMove) canvas.addEventListener(\"pointermove\", onPointerMove);\n\n    return () => {\n      canvas.removeEventListener(\"pointerdown\", onPointerDown);\n      window.removeEventListener(\"pointerup\", onPointerUp);\n      if (onPointerMove) canvas.removeEventListener(\"pointermove\", onPointerMove);\n      // We don't indiscriminately set cursor to auto here on unmount to avoid fighting other biomes\n    };\n  }, [gl, camera]);\n\n  useEffect(() => {\n    return () => {\n      geo.dispose();\n      material.dispose();\n    };\n  }, [geo, material]);\n\n  if (count === 0) return null;\n\n  return (\n    <instancedMesh\n      ref={meshRef}\n      args={[geo, material, count]}\n      frustumCulled={false}\n    />\n  );\n}\n");
